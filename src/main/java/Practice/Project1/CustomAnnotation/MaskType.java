package Practice.Project1.CustomAnnotation;

public enum MaskType {
    GENERIC,   // mask everything except last 4
    EMAIL,     // mask username part of email
    name,     // mask middle digits
    FULL       // mask completely
}
